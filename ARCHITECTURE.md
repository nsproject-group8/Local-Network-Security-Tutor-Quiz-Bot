# 🏗️ System Architecture Diagram

## High-Level Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER INTERFACE                           │
│                     React Frontend (Port 3000)                   │
├─────────────────────────────────────────────────────────────────┤
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────────┐   │
│  │Dashboard │  │Q&A Tutor │  │   Quiz   │  │   Document   │   │
│  │Component │  │Component │  │Component │  │Upload Comp.  │   │
│  └─────┬────┘  └─────┬────┘  └─────┬────┘  └──────┬───────┘   │
│        └──────────────┼─────────────┴───────────────┘           │
│                       │                                          │
│                       │  HTTP/REST API                           │
└───────────────────────┼──────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                   FASTAPI BACKEND (Port 8000)                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    API ENDPOINTS                           │ │
│  │  • /api/qa/ask                                            │ │
│  │  • /api/quiz/generate                                     │ │
│  │  • /api/quiz/grade                                        │ │
│  │  • /api/documents/upload                                  │ │
│  │  • /health                                                │ │
│  └───────────────────────────────────────────────────────────┘ │
│                             │                                    │
│  ┌──────────────────────────┴──────────────────────────────┐   │
│  │                    AGENT LAYER                           │   │
│  │  ┌────────────────────┐    ┌────────────────────┐       │   │
│  │  │   Q&A Tutor Agent  │    │    Quiz Agent      │       │   │
│  │  │                    │    │                    │       │   │
│  │  │ • Question parsing │    │ • MCQ generation   │       │   │
│  │  │ • Context retrieval│    │ • T/F generation   │       │   │
│  │  │ • Answer generation│    │ • Open-Q generation│       │   │
│  │  │ • Citation tracking│    │ • Answer grading   │       │   │
│  │  │ • Web search       │    │ • Feedback creation│       │   │
│  │  └─────────┬──────────┘    └─────────┬──────────┘       │   │
│  └────────────┼───────────────────────────┼──────────────────┘   │
│               │                           │                      │
│  ┌────────────┴───────────────────────────┴──────────────────┐  │
│  │                   SERVICE LAYER                           │  │
│  │  ┌────────────────┐  ┌────────────────┐  ┌────────────┐ │  │
│  │  │   Embedding    │  │     Ollama     │  │  Document  │ │  │
│  │  │    Service     │  │    Service     │  │ Processor  │ │  │
│  │  │                │  │                │  │            │ │  │
│  │  │ • SentenceTr.. │  │ • LLM client   │  │ • PDF ext. │ │  │
│  │  │ • ChromaDB     │  │ • Llama 3.2    │  │ • DOCX ext.│ │  │
│  │  │ • Vector ops   │  │ • Generation   │  │ • PPTX ext.│ │  │
│  │  └────────┬───────┘  └────────┬───────┘  └─────┬──────┘ │  │
│  └───────────┼──────────────────┬─┼─────────────────┼────────┘  │
│              │                  │ │                 │           │
│  ┌───────────┴──────┐  ┌────────┴─┴─────┐  ┌────────┴────────┐ │
│  │  Web Search      │  │  Security Layer │  │   Data Storage  │ │
│  │  • DuckDuckGo    │  │  • Encryption   │  │                 │ │
│  │  • Citation ext. │  │  • Audit log    │  │                 │ │
│  └──────────────────┘  │  • Net monitor  │  │                 │ │
│                        └─────────────────┘  │                 │ │
└─────────────────────────────────────────────┼─────────────────┼─┘
                                              │                 │
                                              ▼                 ▼
                                    ┌─────────────────┐ ┌──────────────┐
                                    │   ChromaDB      │ │   Ollama     │
                                    │  Vector Store   │ │   LLM API    │
                                    │  (Local)        │ │  (Port 11434)│
                                    └─────────────────┘ └──────────────┘
```

## Data Flow

### 1. Q&A Request Flow

```
User Question
    │
    ▼
React Component
    │
    ▼
API Client (axios)
    │
    ▼
FastAPI Endpoint (/api/qa/ask)
    │
    ▼
Q&A Tutor Agent
    │
    ├─► Embedding Service
    │   └─► Generate question embedding
    │
    ├─► ChromaDB
    │   └─► Retrieve relevant chunks
    │
    ├─► (Optional) Web Search
    │   └─► Get supplementary info
    │
    └─► Ollama Service
        └─► Generate answer with context
            │
            ▼
        Extract Citations
            │
            ▼
        Return Response
            │
            ▼
        Display to User
```

### 2. Quiz Generation Flow

```
Quiz Config
    │
    ▼
Quiz Agent
    │
    ├─► Extract Documents
    │   └─► ChromaDB query
    │
    ├─► For each question:
    │   │
    │   ├─► Select random chunk
    │   │
    │   └─► Ollama Service
    │       └─► Generate question
    │           │
    │           ├─► MCQ (4 options)
    │           ├─► T/F (binary)
    │           └─► Open-ended (free form)
    │
    └─► Store Quiz
        └─► Return to User
```

### 3. Quiz Grading Flow

```
User Answers
    │
    ▼
Quiz Agent
    │
    ├─► For MCQ/T/F:
    │   └─► Exact string match
    │       └─► Return grade (A/F)
    │
    └─► For Open-ended:
        │
        ├─► Embedding Service
        │   └─► Generate embeddings
        │       • User answer
        │       • Correct answer
        │
        ├─► Calculate Cosine Similarity
        │   └─► 0.85+ = A
        │   └─► 0.75-0.84 = B
        │   └─► 0.65-0.74 = C
        │   └─► 0.50-0.64 = D
        │   └─► <0.50 = F
        │
        └─► Ollama Service
            └─► Generate detailed feedback
                │
                ▼
            Return grading with citations
```

### 4. Document Upload Flow

```
User Uploads File
    │
    ▼
FastAPI Endpoint
    │
    ├─► Validate file type
    │   └─► (PDF, DOCX, PPTX, TXT, MD)
    │
    ├─► Save to disk
    │   └─► data/uploads/
    │
    ├─► Document Processor
    │   │
    │   ├─► PDF: pypdf
    │   ├─► DOCX: python-docx
    │   ├─► PPTX: python-pptx
    │   └─► TXT/MD: direct read
    │       │
    │       ▼
    │   Extract text chunks
    │       │
    │       ▼
    │   Add metadata
    │
    └─► Embedding Service
        │
        ├─► Generate embeddings
        │   └─► sentence-transformers
        │
        └─► ChromaDB
            └─► Store vectors + metadata
                │
                ▼
            Return success
```

## Security Layer

```
┌────────────────────────────────────────┐
│         SECURITY FEATURES              │
├────────────────────────────────────────┤
│                                        │
│  ┌──────────────────────────────────┐ │
│  │  Encryption Service              │ │
│  │  • Fernet encryption             │ │
│  │  • File encryption               │ │
│  │  • Data at rest protection       │ │
│  └──────────────────────────────────┘ │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │  Audit Logger                    │ │
│  │  • API request logging           │ │
│  │  • Data access tracking          │ │
│  │  • Security event logging        │ │
│  │  • JSON log format               │ │
│  └──────────────────────────────────┘ │
│                                        │
│  ┌──────────────────────────────────┐ │
│  │  Network Monitor                 │ │
│  │  • Connection tracking           │ │
│  │  • Suspicious detection          │ │
│  │  • Local-only verification       │ │
│  │  • Traffic analysis              │ │
│  └──────────────────────────────────┘ │
│                                        │
└────────────────────────────────────────┘
```

## Storage Architecture

```
data/
├── documents/          ← Source documents (user provides)
│   ├── textbook.pdf
│   ├── slides.pptx
│   └── notes.docx
│
├── uploads/            ← Uploaded files (via UI/API)
│   └── [user-uploads]
│
├── chroma_db/          ← Vector database (ChromaDB)
│   ├── [embeddings]
│   ├── [metadata]
│   └── [indices]
│
logs/
├── app.log             ← Application logs
└── audit.log           ← Security audit logs
```

## Component Dependencies

```
Frontend Components
    │
    └─► API Client (axios)
            │
            └─► Backend FastAPI
                    │
                    ├─► Q&A Agent
                    │   ├─► Embedding Service
                    │   │   └─► sentence-transformers
                    │   ├─► ChromaDB
                    │   ├─► Ollama Service
                    │   │   └─► Llama 3.2 (local)
                    │   └─► Web Search
                    │       └─► DuckDuckGo
                    │
                    └─► Quiz Agent
                        ├─► Embedding Service
                        ├─► ChromaDB
                        └─► Ollama Service
```

## Network Topology

```
┌──────────────────────────────────────────────┐
│         Local Machine (Your Computer)         │
│                                               │
│  ┌──────────────┐         ┌───────────────┐ │
│  │   Browser    │         │   Terminal    │ │
│  │ localhost:3000│         │  (Ollama)     │ │
│  └──────┬───────┘         └───────┬───────┘ │
│         │                         │          │
│         │ HTTP                    │          │
│         ▼                         ▼          │
│  ┌──────────────┐         ┌───────────────┐ │
│  │   Frontend   │         │    Ollama     │ │
│  │   (Vite)     │         │    Service    │ │
│  │  Port: 3000  │         │  Port: 11434  │ │
│  └──────┬───────┘         └───────────────┘ │
│         │                         ▲          │
│         │ HTTP                    │          │
│         ▼                         │          │
│  ┌──────────────┐                 │          │
│  │   Backend    │─────────────────┘          │
│  │   (FastAPI)  │                            │
│  │  Port: 8000  │                            │
│  └──────┬───────┘                            │
│         │                                     │
│         │ File System                         │
│         ▼                                     │
│  ┌──────────────┐                            │
│  │  ChromaDB    │                            │
│  │  (Local FS)  │                            │
│  └──────────────┘                            │
│                                               │
│  🔒 All data stays on this machine          │
│  🔐 No external API calls (except web search)│
└──────────────────────────────────────────────┘
```

## Technology Stack Details

```
┌──────────────────────────────────────┐
│         Technology Stack              │
├──────────────────────────────────────┤
│                                       │
│  Frontend:                            │
│  • React 18.2.0                       │
│  • Vite 5.0.8                         │
│  • Axios 1.6.2                        │
│  • React Markdown 9.0.1               │
│  • Lucide React (icons)               │
│                                       │
│  Backend:                             │
│  • Python 3.9+                        │
│  • FastAPI 0.104.1                    │
│  • Uvicorn 0.24.0                     │
│  • Pydantic 2.5.0                     │
│                                       │
│  AI/ML:                               │
│  • sentence-transformers 2.2.2        │
│  • ChromaDB 0.4.18                    │
│  • Ollama 0.1.6                       │
│  • scikit-learn 1.3.2                 │
│                                       │
│  Document Processing:                 │
│  • pypdf 3.17.1                       │
│  • python-docx 1.1.0                  │
│  • python-pptx 0.6.23                 │
│                                       │
│  Security:                            │
│  • cryptography 41.0.7                │
│  • loguru 0.7.2                       │
│  • psutil 5.9.6                       │
│                                       │
└──────────────────────────────────────┘
```

This architecture ensures:
- ✅ Privacy: All processing happens locally
- ✅ Security: Encryption and audit logging
- ✅ Scalability: Modular design
- ✅ Maintainability: Clear separation of concerns
- ✅ Performance: Efficient vector search
